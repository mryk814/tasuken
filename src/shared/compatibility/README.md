# Shared compatibility

Legacy aliases and migration adapters belong here only when their consumers and
removal condition are recorded. New kernel and feature-contract code must not
import this directory.
