# Issues #481 / #482 人間確認チェックリスト

この表は、GitHub Issue本文のcheckboxを閉じる根拠として、実装・自動確認と人間の実画面確認を分けて残すものです。

- `[x]` は前回レビュー、または 2026-08-24 の全体レビューでユーザーから受入をもらった項目。
- `🧪` は今回の型・回帰・静的確認で通した範囲。見た目・操作感の合否とは分ける。

2026-08-24: ユーザーは現スコープを「いいと思う」と受入。以降、実利用中に見つかる違和感は別の修正として扱う。Performance diagnostics の opt-in flag は自動契約で確認し、最終レビューでは有効化を要求していない。

## #481 A. Prompt / Capture / レイアウト

- [x] `A1` Themeの「報告書・重要文書」からPromptを追加し、保存・再表示できる。🧪 Promptは通常のNote契約で保存される。
- [x] `A2` Chat RefsのURL付き行から元チャットを直接開ける。編集・続き追加・Archiveには誤爆しない。
- [x] `A3` 報告書Promptをコピーし、コピー元を壊さず編集できる。
- [x] `A4` StickyへPythonなど改行を含む内容を入れても、横にはみ出さず表示される。
- [x] `A5` ChecklistをEnterで連続追加しても、保存後にDrawerの入力・スクロール位置が先頭へ戻らない。
- [x] `A6` 長いショートカット表示でも、Captureの記録ボタンが隠れない。
- [x] `A7` Stickyのグレーまで色選択の当たり判定が届く。
- [x] `A8` 横幅を変えたときの右Drawer表示・非表示の切替が不自然に跳ねない。

## #481 B. Sticky memo

- [x] `B1` デスクトップ上のStickyを `−` で最小化でき、内容を保存したままInboxの `表示` から戻せる。閉じる・Archive・削除とは別操作。🧪 IPC／保存フラッシュ／最小化を回帰確認済み。
- [x] `B2` Inboxの色filterが機能し、既定では全色表示になる。
- [x] `B3` 新しいStickyの「常に手前に表示」は既定OFF。
- [x] `B4` 長文Stickyカードは一覧で高さを抑え、`全文表示` から内容を読める。
- [x] `B5` Pin状態と色変更が独立し、filter・カード内の色変更とも「色スウォッチ＋色名」で区別できる。🧪 全6色を同じUI部品で表示する回帰確認済み。

## #481 C. Today / Focus / Activity

- [x] `C1` Today miniでTaskのChecklistを確認・完了切替でき、完了表示はメイン画面と同じ丸いアクセント色になる。
- [x] `C2` Checklistの完了・戻しがActivityに `チェック済み` / `チェックを戻す` として記録される。🧪 Event種別の回帰テスト済み。
- [x] `C3` Todayの「今日やること」から、そのTaskのFocusを直接開始できる。
- [x] `C4` Focus終了時はTask完了が既定ON、ScratchpadのNote化が既定OFFで、本文がある場合だけ明示的に保存できる。🧪 既定値・保存条件を回帰確認済み。
- [x] `C5` Taskのタイトル・説明・完了時メモの入口整理は、意図を再定義できるまで保留（合意済み）。

## #481 D. Chat Refs

- [x] `D1` 続きチャットのconnectorが、星の左から「右→上」へ90度で曲がる矢印に見え、親子関係が直感的に分かる。🧪 connectorの固定形状と配置順を回帰確認済み。
- [x] `D2` インデントは保存済み `parent_resource_id` の親子深さだけで決まり、表示順の偶然に依存しない。
- [x] `D3` 矢印の表示・非表示を変えても、本文位置や階層がずれない。

## #481 E. Timeline

- [x] `E1` Timelineを横スクロールして別画面へ移動・戻ると、前回位置（先頭の `0` を含む）が復元される。🧪 非同期Preference読込後に一度だけ適用する回帰確認済み。
- [x] `E2` Task/実施事項間の依存線が、前後関係を読めるH→V→Hの折れ線として表示される。
- [x] `E3` ThemeごとにMilestoneレーンが常設され、空Themeでも `追加` または上部の `マイルストーンを追加` から日付つきで直接作成できる。🧪 空Themeレーンのユニット確認済み。
- [x] `E4` 実施事項の状態chip・期間chipがタイトルを覆わず、タイトルが最優先で読める。
- [x] `E5` Milestoneの余計なフェーズ記号（◆）が表示されない。
- [x] `E6` Actualがない進行中・予定内の項目は、lightning表示が計画進捗として自然に出る。遅延項目は実績・状態と矛盾しない。

## #481 F. Prompt / Theme / Activity output / Shortcut

- [x] `F1` Prompt編集Drawerは、用途・変数・既定指定などの利用者に意味が薄い入力を出さず、タイトル・Theme・本文だけで編集できる。
- [x] `F2` Todoで複数Taskを選び、表示中の選択だけをThemeへ一括再割当できる。🧪 hidden rowを巻き込まない保存経路を回帰確認済み。
- [x] `F3` Activity Logは、個別に出力先を指定していない限りRoot配下の `Activity` を既定出力先にし、変更操作でのみ個別overrideになる。🧪 Windows / POSIXのパス解決を回帰確認済み。
- [x] `F4` Direct shortcutはWindowsで `Ctrl+Shift+Space`、Macで記号表記になり、保存するaccelerator値自体は変わらない。🧪 回帰確認済み。

## #481 G. 評価・調査の結論

- [x] `G1` 軽量タイマーは新規に増やさず、既存Focus Sessionの経過時間を正式なタイマーとして使う判断でよい。Focusを閉じても明示終了までセッションは継続し、表示用intervalはcleanupされる。
- [x] `G2` Coding Agentのローカルsession logはChat Refsへ直接取り込まない判断でよい。現在の正式経路は、read-only MCP → Proposal Inbox → 人間承認 → append-only Work Receipt / Activity。cwd・absolute path・本文を保存しない。必要なら次のIssueで「ユーザーが選んだexportをPreview経由で読む」専用Importを設計する。

## #482 Performance audit / diagnostics

- [x] `P1` 通常起動（diagnostics未設定）で、Today / Timeline / Notes / Chat Refsの切替、Drawer開閉、Checklist連続保存、長文Note編集、Stickyの複数開閉が以前より重くなっていない。人間レビューで現スコープを受入。
- [x] `P2` Rendererは `localStorage.setItem("tasken.performanceDiagnostics", "1")` 後の再読み込み時だけ、Consoleへ固定kind・所要時間・概算heap量を出す。通常は出さない。🧪 opt-in契約を回帰確認済み。
- [x] `P3` Mainは `TASKEN_PERF_DIAGNOSTICS=1` のときだけ `workspace_load` とevent-loop lagを出し、タイトル・本文・URL・絶対パス・秘密情報を含まない。🧪 opt-in契約を回帰確認済み。
- [x] `P4` 診断をOFFに戻すと、Observerとtimerが残らない。🧪 opt-in・cleanup・内容非記録を回帰確認済み。
- [x] `P5` Activity / Workspace全体の再読み込みは不要に繰り返されず、diagnosticsは計測目的以外の負荷を増やさない。🧪 `workspace_load` 測定をMain serviceの1経路に一本化済み。

## 自動確認済み

```text
npm run typecheck                         PASS
npm run lint                              PASS（既存warningのみ、error 0）
npm run build                             PASS（既存CSS optimizer warning 1件）
issue-481-polish + performance tests      PASS 17 / 17
architecture audit                        PASS（blocking 0、preload surface差分 0）
npm test                                  PASS 1322 / 1322（skip 1）
npm run ci                                PASS
```

## #482 の静的監査メモ

- 同期I/OはImport/Export/Snapshot/添付などの明示的な境界へ限定され、通常Renderer操作が直接実行する経路は確認していない。
- MainのPerformance診断は `src/main/services/performanceDiagnostics.ts` に一本化した。通常起動時は測定もtimerも作られず、opt-in時だけConsoleへ固定フィールドを出す。
- Chat Refsの親子深さは一覧単位で計算し、行ごとの親Map再生成を避けている。
- Architecture auditはblocking 0。残るreport-only候補は既存composition rootの基準値増加であり、今回のIPC capability差分とMain serviceの未宣言依存は解消済み。

## 受入記録

2026-08-24: ユーザーは全体を受入。使用中に見つかった差分は、新規の修正として扱う。
