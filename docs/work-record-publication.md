# 作業の記録をOneDriveから振り返る

2026-09-05の利用者との確認を反映する。
Taskenの目的は、ローカルで何を試し、何が分かり、何が残ったかを記録し、メールやTeamsの情報と合わせて後から振り返れるようにすることである。
利用者はAI Packを使用していない。
今回の開発では、通常の成果文書とActivityの日誌を中心に据える。

## 保存先ごとの役割

| 保存するもの                     | 現在の保存経路                                                  | 用途                                                                   |
| -------------------------------- | --------------------------------------------------------------- | ---------------------------------------------------------------------- |
| Task・Noteの管理情報、履歴、設定 | 各端末のuserData配下のSQLite                                    | アプリが管理する正式データ。検索向けの文章ではない                     |
| 端末間の変更差分                 | `sharedSyncDirectory/devices/`                                  | SQLite間の同期用。Copilotに読ませる成果物とは分ける                    |
| 通常NoteのMarkdownと成果ファイル | `artifactDirectory`配下のTheme Notes等、または選択した出力先    | 人が読み、外部から参照する文書。canonical Markdownの既存契約を維持する |
| 日ごとのActivity Markdown        | 個別設定した`activityLogDirectory`、未指定ならRootの`Activity/` | 公開してよい作業と結果を日付から振り返るための文章                     |
| Theme AI Pack                    | 既存の明示的な生成操作                                          | 任意機能。通常の記録・日誌出力の必須段階にはしない                     |

RootがOneDrive上なら、そこへ保存された文書はOneDriveへの同期対象になる。
同期用フォルダと読ませる文書のフォルダは役割が違うため、同じ設定として扱わない。
参照させる範囲は主に保存先で決める。通常Noteや日誌に項目別の公開・非公開設定を要求しない。
保存場所の移動、Boxへの同期先変更、実データの一括削除は行わない。

## 今回直す日常の経路

1. **保存先に日誌を出す。** コピー・手動出力・自動出力は同じ生成処理を使う。項目・Theme・全体のAI公開設定で日常の記録を間引かない。資格情報や内部実行情報を転記しない処理は維持する。
2. **定時後の作業を翌日にも反映する。** 当日は指定時刻に暫定出力し、翌日に同じファイルへ再出力する。停止中の未確定日も順番に補完する。出力成功後だけ確定日を保存し、失敗時は再試行対象を残す。
3. **採用したAIの作業結果を日誌へ出す。** Taskと受入イベントのReceipt IDが一致する報告だけを、受入日の「採用済みの作業結果」へ載せる。結果・実施項目・変更物・検証・残作業を区別し、内部の実行情報や資格情報を本文へ転記しない。

AIセッションの終了報告と、人が採用したTaskの完了報告は別の記録として表示する。
MCPや明示的なAI Context生成にある既存の制限は、通常の日誌出力とは別の契約として維持する。

新しい確定日の設定は`activityLogLastFinalizedDate`。
従来の当日出力済み設定`activityLogLastAutoExportDate`を保持し、初回は従来最後に出した日を確定し直す。
同じ日の再出力は既存の日誌ファイルを更新する。
既に外へ出した過去の全ファイルを一括回収する機能ではない。
過去の日誌は、該当日を選んで手動出力すると現在の記録から作り直せる。

## Noteと対応ファイルの寿命

Noteを保存したときに紐づく正本Markdownは、Noteと一緒に管理する。
Noteの削除では対応ファイルも保存先から取り除き、UndoではNoteとファイルを復元する。
OneDriveを保存先にしている場合も、同じファイル操作が同期対象になる。
外部編集されたファイルや、復元先に新しく置かれた別ファイルを無断で上書きしない。
削除・復元の失敗を成功として表示せず、再試行に必要なデータを残す。

対象はNoteに紐づく正本Markdownであり、手動コピー、PDF、添付ファイル、過去の日誌内の言及を一括削除する機能ではない。
Noteを消したことと、その日の作業履歴を消すことは別に扱う。
復元用のファイル内容は端末の`userData/deleted-note-files/`に保持する。
既に対応ファイルが消えている場合は、保存先フォルダが利用可能ならNoteを削除でき、Undoでも元のファイル不在を維持する。
今回接続したのはデスクトップのNote削除・Undoであり、別端末から共有同期で届く削除へのファイル追従は未対応である。

## 既存機能を見直す判断

AI Packは今回拡張しない。
削除する前に、通常Noteの保存・Activityの日誌・成果への参照だけで必要な振り返りが成立するかを見る。
既存の出力物や利用可能なデータを消すことは、使っていない操作を整理することとは分けて扱う。

次に確認すべき点は、通常Noteの`canonical_markdown`保存先から日誌の成果参照への接続である。
現在のActivityの参照は`canonical_ref(s)`を中心に扱い、通常保存のbindingだけでは辿れない可能性がある。
これは今回の3点とは別の残作業として記録する。
必要なのは新しいパック生成ではなく、保存した成果文書へ既存の記録から戻れる経路である。

## 検証境界

Noteと正本Markdownの削除・復元、外部変更との競合、未採用と採用済み報告、翌日の再出力、失敗時の再試行を代表fixtureで検証する。
実OneDriveやM365 Copilotへのアクセス・実検索は今回の対象外である。
Android専用のIssue #518は、利用者の訂正により保留した。

### 先行実装の検証記録（816fd8ec）

以下は項目別公開フィルタを含んでいた先行実装の結果であり、上記の保存先中心の方針へ修正する前の記録である。

- `npm run typecheck`、`npm run build`、`npm run build:mcp`が成功した。
- Activity・公開制限・Receipt・日次確定・SQLite設定・文書出力・Mobile Gateway・共有フォルダ同期・MCP Activityの関連141テストが成功した。完了/再開・Undo・再送・競合の既存テストも保持した。
- 分離したElectronの検証DBに公開/非公開の記録を保存し、Debriefの画面には両方が残ること、出力したMarkdownには公開側だけが入ることを確認した。
- 9月4日23:20の追記と同日の暫定日誌をfixtureとして用意した。再起動後に日誌が追記を含む内容へ更新され、確定日`2026-09-04`が保存されたことを確認した。
- Windowsのunpackedパッケージを生成し、同じ検証データの再表示・手動出力を確認した。NSIS installerの配布や実端末への導入は行っていない。
- [パッケージ版の画面](../output/work-publication-qa/activity-packaged.png)を確認し、Activityの操作と出力設定の文字・配置に崩れがないことを確認した。
- buildには既存の`::highlight` CSS警告、対象ESLintには既存の未使用import/制御文字正規表現の警告がある。今回の対象にはエラーがない。

実OneDriveの検索反映、M365 Copilotによる検索結果、Androidの実画面は未検証。
出力済みの過去の日誌の自動回収は行わない。
今回の検証ファイルとDBは`output/work-publication-qa/`に分離しており、個人の常用データには書き込んでいない。
最終パッケージは`output/work-publication-package-final/win-unpacked/Tasken.exe`。
変更は`codex/work-record-publication`ブランチへ記録する。push・PR作成・配布は行っていない。

### 保存先中心の方針への修正

- 日誌の項目別・Theme別AI公開フィルタを撤去。未所属のAIセッションも記録し、採用済みReceiptとの紐付けと秘密情報の除去は維持した。
- Noteと正本Markdownの削除・Undoを接続し、復旧失敗時も他の記録を開けるようにした。
- 日誌関連33テスト、Noteの正本保存・削除・復旧関連32テスト、同期・Mobile Gateway・Activity等の既存109テストが成功した。
- 最終ソースの`npm run typecheck`、`npm run build`、`npm run build:mcp`が成功した。既存のCSS警告は継続している。
- 分離したElectronの検証データで、画面からNote作成・本文保存・削除・Undoを実行し、対応Markdownの消失と本文の復元を確認した。同名の別ファイルがある場合は保護され、移動後のUndo再試行で復元できた。
- 最終Windowsパッケージでも削除・Undoを実行し、削除後の再起動でNoteとファイルが復活しないことを確認した。[復元後の画面](../output/note-lifecycle-qa/note-undo-packaged.png)で、本文・操作・成功通知の表示を確認した。成果物は`output/note-lifecycle-package-final/win-unpacked/Tasken.exe`。
- 同パッケージのDebriefから日誌を出力し、AI公開設定が空の代表記録も題名・要約が掲載されることを確認した。[出力後の画面](../output/note-lifecycle-qa/activity-folder-packaged.png)で新しい保存先の説明と操作を確認した。
- 対象ESLintはエラー0、警告21。Ponytail reviewで不要な抽象化・依存追加は見つからなかった。
- 個人の常用DBや実OneDriveには書き込んでいない。検証データは`output/note-lifecycle-qa/`に分離した。
